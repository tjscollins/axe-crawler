import fs from 'fs';
import crawlerOpts from '../src/config';

describe('crawlerOpts', () => {
  it('should parse config file correctly', () => {

  });

  it('should parse cmd line opts correctly', () => {

  });
});

